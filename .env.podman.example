# OpenClaw Podman Configuration Example
# ======================================
# Copy this file to .env and customize the values:
#   cp .env.podman.example .env
#   # Edit .env with your settings
#
# The podman-setup.sh script will read and update these values.
#
# TIP: Create a shell alias for shorter commands:
#   alias pc='podman compose -f docker-compose.yml -f docker-compose.podman.yml'
#   Then use: pc up -d, pc logs -f, pc restart, etc.

# ============================================================================
# Directory Configuration
# ============================================================================

# Directory for OpenClaw configuration and credentials
# Default: $HOME/.openclaw
# Note: Use absolute paths or shell variables (e.g., $HOME)
OPENCLAW_CONFIG_DIR=$HOME/.openclaw

# Directory for workspace files (where agents can read/write)
# Default: $HOME/.openclaw/workspace
OPENCLAW_WORKSPACE_DIR=$HOME/.openclaw/workspace

# ============================================================================
# Gateway Configuration
# ============================================================================

# Gateway port (exposed to network)
# Default: 18789
OPENCLAW_GATEWAY_PORT=18789

# Bridge port (internal communication)
# Default: 18790
OPENCLAW_BRIDGE_PORT=18790

# Gateway bind address
# - 'lan': Bind to all interfaces (0.0.0.0) - accessible from network
# - 'loopback': Bind to 127.0.0.1 only - local access only (use with SSH tunnel)
# Default: lan
OPENCLAW_GATEWAY_BIND=lan

# Gateway authentication token (auto-generated if not set)
# Leave blank to auto-generate a secure token during setup
# Or set your own: OPENCLAW_GATEWAY_TOKEN=$(openssl rand -hex 32)
OPENCLAW_GATEWAY_TOKEN=

# ============================================================================
# Container Configuration
# ============================================================================

# Container image name
# Default: openclaw:local
OPENCLAW_IMAGE=openclaw:local

# Additional apt packages to install in the container (space-separated)
# Example: git vim curl jq
# Note: These are installed at build time - must rebuild image if changed
OPENCLAW_DOCKER_APT_PACKAGES=

# ============================================================================
# Advanced Volume Configuration
# ============================================================================

# Extra volume mounts (comma-separated)
# Use this to mount additional directories (e.g., custom MCP servers)
# Example: /opt/mcp-servers:/mcp-servers:ro,/data/shared:/shared
# Format: host-path:container-path[:options]
OPENCLAW_EXTRA_MOUNTS=

# Named volume for persistent home directory (optional)
# Leave blank to use bind mounts only (recommended for most users)
# Only use this if you need Docker-style named volumes
OPENCLAW_HOME_VOLUME=

# ============================================================================
# Model Provider Credentials
# ============================================================================

# ANTHROPIC API KEY (Recommended)
# ----------------------------------
# For most users, you'll use the Anthropic API with an API key.
# This is NOT set here - it goes in your openclaw.json config file:
#   {
#     "providers": {
#       "anthropic": {
#         "apiKey": "sk-ant-api03-..."
#       }
#     }
#   }
# Get your API key from: https://console.anthropic.com/settings/keys

# CLAUDE WEB PROVIDER (Advanced/Optional)
# ----------------------------------------
# These are only needed if you're using the web provider (claude.ai login)
# instead of the Anthropic API. Most users should skip this.
# Get these from https://claude.ai browser dev tools > Application > Cookies
CLAUDE_AI_SESSION_KEY=
CLAUDE_WEB_SESSION_KEY=
CLAUDE_WEB_COOKIE=
